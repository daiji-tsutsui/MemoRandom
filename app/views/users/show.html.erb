<% provide(:title, @user.name) %>

<div class="row">
  <div class="col-md-4">
    <% if current_user?(@user) %>
      <%= link_to 'Edit profile', edit_user_path(@user) %> |
    <% end %>
    <%= link_to 'Back', users_path %>
  </div>
  <div class="col-md-8"></div>
</div>
<br>

<%= render 'shared/post_list', posts: @posts, user: @user %>
