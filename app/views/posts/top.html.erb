<p id="notice"><%= notice %></p>

<h1>Top</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @users.each do |user| %>
      <tr>
        <td><%#= user.name %></td>
        <td><%= link_to 'Show', post_path(1) %></td>
        <td><%= link_to 'Edit', edit_post_path(1) %></td>
        <td><%#= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
